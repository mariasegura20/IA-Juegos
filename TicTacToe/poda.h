// tNodo *poda_ab(tNodo * t)
// Funcion que calcula la mejor jugada mediante la poda alfa-beta
// t: nodo que contiene el tablero
// devuelve el nodo que contiene el tablero con la jugada implementada
tNodo *poda_ab(tNodo * nodo);